const API_VERSION = "/api/v1";

export const NEWS = `${API_VERSION}/news`;
export const EVENT = `${API_VERSION}/event`;
export const BOARD = `${API_VERSION}/board`;
export const POSITION = `${API_VERSION}/position`;
export const SALES_MANAGEMENT = `${API_VERSION}/sales-management`;
export const PRODUCT = `${API_VERSION}/products`;
export const PRODUCT_STOCK = `${API_VERSION}/product-stock`;
export const STUDENT = `${API_VERSION}/student`;

export const LOGIN = `${API_VERSION}/auth/login`;
export const LOGOUT = `${API_VERSION}/auth/logout`;
export const CHANGE_PASSWORD = `${API_VERSION}/auth/change-password`;

export const IMAGE_UPLOAD = `${API_VERSION}/order/visitation/upload`;

export const VISITATION = `${API_VERSION}/order/visitation`;
export const VISITATION_SALES_SEARCH = `${API_VERSION}/user/sales`;
export const VISITATION_STORES_SEARCH = `${API_VERSION}/store`;

export const COURSE = `${API_VERSION}/course`;

export const TOPIC = `${API_VERSION}/topic`;
export const WIKI = `${API_VERSION}/wiki`;
